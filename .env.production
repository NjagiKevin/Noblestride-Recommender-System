# Production Environment Variables for External Database Setup

# Main Application Database (Your existing Noblestride microservice database)
# Update these with your actual database connection details
DB_HOST=your-noblestride-db-host
DB_USER=your-db-username
DB_PASSWORD=your-db-password
DB_NAME=noblestride
DB_PORT=5432
DATABASE_URL=postgresql+psycopg2://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Application Settings
APP_NAME=RecommenderService
APP_VERSION=1.0.0
ENVIRONMENT=production
LOG_LEVEL=INFO

# Service Ports
API_PORT=8010
AIRFLOW_PORT=8090
MLFLOW_PORT=5000

# Airflow Configuration (External Database for Airflow metadata)
AIRFLOW_DB_HOST=your-airflow-db-host
AIRFLOW_DB_USER=airflow
AIRFLOW_DB_PASSWORD=secure-airflow-password
AIRFLOW_DB_NAME=airflow
AIRFLOW_DB_PORT=5432

# Airflow Web UI Credentials
AIRFLOW_WEB_USER=admin
AIRFLOW_WEB_PASSWORD=secure-admin-password-2024

# MLflow Configuration (External Database for MLflow tracking)
MLFLOW_DB_HOST=your-mlflow-db-host
MLFLOW_DB_USER=mlflow
MLFLOW_DB_PASSWORD=secure-mlflow-password
MLFLOW_DB_NAME=mlflow
MLFLOW_DB_PORT=5432
MLFLOW_TRACKING_URI=http://mlflow:5000

# Redis Configuration (Internal Redis on port 6380)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=secure-redis-password

# Security Keys (Generate new ones for production)
FERNET_KEY=your-generated-fernet-key-here
SECRET_KEY=your-super-secret-key-for-production-2024

# API Configuration  
AIRFLOW_API_URL=http://airflow-webserver:8080
AIRFLOW_API_USERNAME=admin
AIRFLOW_API_PASSWORD=secure-admin-password-2024

# External Service URLs (if needed)
NODEJS_BASE_URL=http://your-nodejs-service:3030
FASTAPI_BASE_URL=http://recommender_api:8000